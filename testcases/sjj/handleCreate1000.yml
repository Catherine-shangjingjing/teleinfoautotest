config:
    name: 创建1000条标识数据
#    parameters:
#        biaoshi: ${parameterize(data/parames10.csv)}
    parameters:
        biaoshi: ${get_dict(1000)}
    variables: {
#        handle: "$prefix/${get_random_param(100000,1000000,1)}",
        handle: "$prefix/$biaoshi",
        version: "zjauto${get_random_param(10000,1000000,1)}",
        prefix: "${get_prefix()}",
        account: "${ENV(adminaccount)}",
        password: "${ENV(adminpassword)}",
        session: "${get_session($account, $password)}",
        email: "${get_email()}",
        mobile: "${get_mobile()}",
        username: "${get_username()}",
        entaccount: "$username",
        entpassword: "${ENV(entpassword)}",
        entsession: "${get_session($entaccount, $entpassword)}"
    }
teststeps:
-   name: 企业申请托管并审核通过
    testcase: testsuites/sjj/proxyserveraudit.yml
#-   name: 管理员用户首页统计数据
#    testcase: testcases/statistics/indexstatistics.yml
-   name: 数据模板创建
    testcase: testcases/zj/templateCreate.yml
-   name: 标识注册
    testcase: testcases/handle/handleCreate1000.yml




