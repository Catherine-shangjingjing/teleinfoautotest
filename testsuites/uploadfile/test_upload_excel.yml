config:
    name: excel上传

    variables:
        entaccount: "${ENV(entaccount)}"
        entpassword: "${ENV(entpassword)}"
        fileName: "${ENV(excelFileName)}"
teststeps:

-   name: 登录-企业
    testcase: testcases/login/login-ent.yml

-   name: 上传excel文件
    testcase: testcases/uploadfile/UploadExcel.yml





