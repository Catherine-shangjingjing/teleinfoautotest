# NOTE: Generated By HttpRunner v3.1.6
# FROM: testsuites/checkhandle/checkId_exist.yml


import sys
from pathlib import Path

sys.path.insert(0, str(Path(__file__).parent.parent.parent))


from httprunner import HttpRunner, Config, Step, RunRequest, RunTestCase

from testcases.checkhandle.checkId_exist_test import (
    TestCaseCheckidExist as CheckidExist,
)


class TestCaseCheckidExist(HttpRunner):

    config = Config("门户查询不存在的标识")

    teststeps = [
        Step(RunTestCase("门户查询标识信息不存在").call(CheckidExist)),
    ]


if __name__ == "__main__":
    TestCaseCheckidExist().test_start()
