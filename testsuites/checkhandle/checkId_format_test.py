# NOTE: Generated By HttpRunner v3.1.6
# FROM: testsuites/checkhandle/checkId_format.yml


import sys
from pathlib import Path

sys.path.insert(0, str(Path(__file__).parent.parent.parent))


from httprunner import HttpRunner, Config, Step, RunRequest, RunTestCase

from testcases.checkhandle.checkId_format_test import (
    TestCaseCheckidFormat as CheckidFormat,
)


class TestCaseCheckidFormat(HttpRunner):

    config = Config("门户查询格式错误的标识信息用例集")

    teststeps = [
        Step(RunTestCase("门户查询标识信息格式错误").call(CheckidFormat)),
    ]


if __name__ == "__main__":
    TestCaseCheckidFormat().test_start()
